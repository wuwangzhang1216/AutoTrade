import type { IconSize } from '../type';
export declare const calcSize: (iconSize?: IconSize) => {
    size: number;
    strokeWidth?: undefined;
} | {
    size: string | number;
    strokeWidth: string | number;
};
